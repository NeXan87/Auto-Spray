# AutoSpray — Умный освежитель воздуха для туалета с глубоким сном и пробуждением по свету

**Энергоэффективное устройство, которое автоматически распыляет ароматизатор в туалете — только когда это нужно.**  
Спящий режим потребляет **менее 0.1 мкА** — работает от аккумулятора 18650 **до 1 года** без подзарядки.

---

## 💡 Как это работает?

- **Автоматический режим**:  
  → При включении света в туалете устройство начинает отсчёт **3 минут**.  
  → Как только свет **выключается**, происходит **один или два импульса распыления** (на 4 мс каждый).  
  → После распыления — **блокировка на 15 минут** (чтобы не перерасходовать ароматизатор).  

- **Ручной режим**:  
  → Нажмите кнопку — устройство распылит ароматизатор немедленно.  

- **Режим «по таймеру» (опционально)**:  
  → Если раскомментировать `#define SPRAY_ON_TIMER_ONLY` — распыление происходит **сразу после 3 минут света**, без ожидания выключения.  

- **Максимальная экономия энергии**:  
  → Микроконтроллер **спит в `SLEEP_MODE_PWR_DOWN`**, пока свет выключен и нет блокировки.  
  → Пробуждается **мгновенно** по:  
    - Нажатию кнопки (D2),  
    - Изменению освещённости (цифровой выход фоторезистора на D3).  
  → При активном свете — работает, но **только если нужно**.  

---

## 🔌 Подключение (без изменений схемы!)

| Компонент | Пин Pro Micro |
|----------|----------------|
| Кнопка (GND при нажатии) | **D2** |
| Цифровой выход фоторезистора (HIGH = светло) | **D3** |
| Мотор распылителя | **D4** |
| Красный LED (блокировка) | **D5** |
| Переключатель режима (LOW = автоматический) | **D6** |
| Зелёный LED (работа) | **D7** |
| Синий LED (готовность) | **D8** |
| Пьезоизлучатель | **D9** |

> ✅ Подходит для **Arduino Pro Micro** (ATmega32U4) и **цифровых модулей фоторезистора** (например, KY-018).  
> ❌ Не требует внешнего питания — работает от батареек 3–5 В.

---

## ⚙️ Настройка

1. Установите библиотеку `Bounce2`.  
2. Откройте `AutoSpray.ino` в Arduino IDE.  
3. **Для режима «по таймеру»** — раскомментируйте:  
   ```cpp
   #define SPRAY_ON_TIMER_ONLY
